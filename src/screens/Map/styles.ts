import styled from "styled-components/native";

const MapMainContainer = styled.View`
  flex: 1;
  position: relative;
`;

const MapTravel = styled.View`
  flex: 1;
`;

export {
  MapMainContainer,
  MapTravel
}